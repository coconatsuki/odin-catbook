$("#likes-count-<%= @post.id %>").html("<%= @post.likes.count %> people like this.");
$("#likes-<%= @post.id %>").html("<%= escape_javascript(render(partial: 'likes/unlike_form', locals: { post: @post, like: @like })) %>");
