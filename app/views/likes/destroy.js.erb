$("#likes-<%= @post.id %>").html("<%= escape_javascript(render(partial: 'likes/likes_form', locals: { post: @post })) %>");
$("#likes-count-<%= @post.id %>").html("<%= @post.likes.count %> people like this.");
